# Session Boot Prompt

Use the generated bundle from `generated/project-context-bundle.md`.

Then:

1. confirm identity
2. confirm current user context
3. confirm layered memory
4. confirm today's daily memory
5. confirm active lessons

Do not broad-scan the entire memory workspace if the generated bundle is enough.
